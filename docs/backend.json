
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données de compte des utilisateurs enregistrées dans Firestore.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "CompleteName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "lastLogin": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce de produit déposée par un vendeur.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string", "enum": ["new", "used", "refurbished"] },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "active", "rejected", "sold"] },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "Accès Profil. Lecture: Tout utilisateur authentifié. Écriture: Le propriétaire (auth.uid == userId) ou l'admin (ndaw22@gmail.com)."
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "Annonces produits. Lecture publique autorisée pour TOUS (get et list). Création: Tout utilisateur authentifié. Modification/Suppression: Le vendeur ou l'admin (ndaw22@gmail.com)."
    }
  }
}
