
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données utilisateur.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "lastLogin": { "type": "string" }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce produit.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "L'utilisateur et l'admin ndaw22@gmail.com ont un accès complet. ALLOW READ, WRITE: if request.auth != null && (request.auth.uid == userId || request.auth.token.email == 'ndaw22@gmail.com')"
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "LECTURE PUBLIQUE : Tout le monde peut lister (list) et lire (get) si le statut est 'active'. PROPRIÉTAIRE : L'utilisateur (sellerId) peut créer, modifier et supprimer ses produits. ADMIN : ndaw22@gmail.com a tous les droits. ALLOW READ: if resource.data.status == 'active' || (request.auth != null && (request.auth.uid == resource.data.sellerId || request.auth.token.email == 'ndaw22@gmail.com')); ALLOW CREATE: if request.auth != null; ALLOW UPDATE, DELETE: if request.auth != null && (request.auth.uid == resource.data.sellerId || request.auth.token.email == 'ndaw22@gmail.com')"
    }
  }
}
