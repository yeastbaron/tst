
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données utilisateur. Chaque utilisateur peut gérer son propre profil.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "lastLogin": { "type": "string" }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce produit. Lecture publique si actif.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "ALLOW READ, WRITE: if request.auth.uid == userId || request.auth.token.email == 'ndaw22@gmail.com'"
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "ALLOW READ: if resource.data.status == 'active' || request.auth.uid == resource.data.sellerId || request.auth.token.email == 'ndaw22@gmail.com'; ALLOW CREATE: if request.auth != null; ALLOW UPDATE, DELETE: if request.auth.uid == resource.data.sellerId || request.auth.token.email == 'ndaw22@gmail.com'"
    }
  }
}
