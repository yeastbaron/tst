
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données spécifiques à l'utilisateur et rôles.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce d'un produit dans la marketplace.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "LECTURE/ÉCRITURE : Autorisé uniquement pour le propriétaire du compte (userId == auth.uid) ou l'administrateur (ndaw22@gmail.com)."
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "LECTURE : Accès public autorisé pour tous si status == 'active'. Les propriétaires (sellerId == auth.uid) peuvent lire leurs propres annonces quel que soit le statut. L'administrateur (ndaw22@gmail.com) peut lire toutes les annonces. ÉCRITURE : Création autorisée pour tout utilisateur authentifié. Mise à jour et suppression autorisées uniquement pour le propriétaire de l'annonce (sellerId == auth.uid) ou l'administrateur (ndaw22@gmail.com)."
    }
  }
}
