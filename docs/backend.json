
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données spécifiques à l'utilisateur et rôles.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "lastLogin": { "type": "string" }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce d'un produit dans la marketplace.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "AUTORISATION : Tout utilisateur authentifié peut créer, lire et modifier son propre profil. L'admin (ndaw22@gmail.com) a un accès complet."
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "AUTORISATION : Lecture autorisée pour tous si status == 'active'. Les propriétaires (sellerId == auth.uid) peuvent voir, créer et modifier leurs propres annonces. L'admin (ndaw22@gmail.com) peut voir et valider (status) toutes les annonces."
    }
  }
}
