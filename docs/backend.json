
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données de compte des utilisateurs enregistrées dans Firestore.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "CompleteName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "lastLogin": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce de produit déposée par un vendeur.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string", "enum": ["new", "used", "refurbished"] },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "active", "rejected", "sold"] },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserProfile" },
      "description": "User profile access. READ: Any authenticated user can read profiles. WRITE: Only the owner (userId matches auth.uid) or the admin ndaw22@gmail.com can write."
    },
    "/products/{productId}": {
      "schema": { "$ref": "#/backend/entities/Product" },
      "description": "Marketplace ads. READ: Public access for everyone. CREATE: Any authenticated user can create an ad. UPDATE/DELETE: Only the seller (sellerId field matches auth.uid) or the admin ndaw22@gmail.com can update or delete."
    }
  }
}
