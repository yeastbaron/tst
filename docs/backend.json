
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données spécifiques à l'utilisateur et rôles.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce d'un produit dans la marketplace.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "LECTURE/ÉCRITURE : Autorisé pour le propriétaire du profil (userId == auth.uid) ou l'administrateur (ndaw22@gmail.com)."
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "LECTURE : Publique pour tous si status == 'active'. ÉCRITURE : Création autorisée pour tout utilisateur authentifié (auth != null). Mise à jour/Suppression autorisée uniquement pour le propriétaire (sellerId == auth.uid) ou l'administrateur (ndaw22@gmail.com)."
    }
  }
}
