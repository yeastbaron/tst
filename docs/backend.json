
{
  "entities": {
    "UserProfile": {
      "title": "Profil Utilisateur",
      "description": "Données utilisateur. Chaque utilisateur peut gérer son propre profil.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "lastLogin": { "type": "string" }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Produit",
      "description": "Annonce produit. Lecture publique si actif. Création autorisée pour tout utilisateur connecté.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "basePrice": { "type": "number" },
        "condition": { "type": "string" },
        "category": { "type": "string" },
        "images": { "type": "array", "items": { "type": "string" } },
        "sellerId": { "type": "string" },
        "status": { "type": "string" },
        "createdAt": { "type": "object" }
      },
      "required": ["title", "basePrice", "sellerId", "status"]
    }
  },
  "auth": {
    "providers": ["google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "#/backend/entities/UserProfile",
      "description": "RÈGLES : Tout utilisateur authentifié peut 'write' son propre document si son uid correspond à userId. L'admin a accès à tout."
    },
    "/products/{productId}": {
      "schema": "#/backend/entities/Product",
      "description": "RÈGLES : Lecture autorisée pour tous si status == 'active'. Les utilisateurs connectés peuvent 'create'. L'auteur (sellerId) peut modifier."
    }
  }
}
